part of 'pages.dart';

class DetailArticle extends StatelessWidget {
  const DetailArticle({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text("ini detail")
      ),
    );
  }
}